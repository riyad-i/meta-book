import { useParams } from "react-router-dom"

export default function ViewPostPage(){
    const params= useParams()
    const id = params.id
    
    return(
        <div>
        
        </div>
    )
}